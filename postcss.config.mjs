/** @type {import('postcss-load-config').Config} */
const config = {
	plugins: {
		tailwindcss: {},
		// 'postcss-modules': {
		// 	generateScopedName: '[hash:base64:5]'
		// }
	},
	// 'autoprefixer': {}
}

export default config
